A simple text generator which uses a Markov Chain to generate text of a desired length from a selected sample text. User specifies the length of the output text and the sample text.

Comes with 4 sample texts and the option to input a custom text
